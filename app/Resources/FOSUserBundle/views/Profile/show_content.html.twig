{% trans_default_domain 'FOSUserBundle' %}

<div class="fos_user_user_show">
    <p>Password changed for user: {{ user.email }}</p>
    <a href="{{ path('homepage') }}" class="btn btn-primary">Back</a>
</div>
