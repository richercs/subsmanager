{% trans_default_domain 'FOSUserBundle' %}

<div class="fos_user_user_show">
    <p>Felhasználó: {{ user.email }} jelszava sikeresen megváltozott!</p>
    <a href="{{ path('homepage') }}" class="btn btn-primary">Vissza</a>
</div>
