{% extends 'subscription/layout.html.twig' %}

{% block body %}
    {{ parent() }}
    <h3>Edit Subscription</h3>
    <div id="yellow-wrapper">
        {{ form(form) }}
        <a href="{{ path('subscription_list_all') }}" class="btn btn-primary">Back</a>
    </div>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}