{% extends 'users/layout.html.twig' %}

{% block body %}
    {{ parent() }}
    <h3>Edit User</h3>
    <div id="yellow-wrapper">
        {{ form(form) }}
        {%  if logged_in_user.getIsAdmin %}
            <a href="{{ path('useraccount_list_all') }}" class="btn btn-primary">Back</a>
        {% else %}
            <a href="{{ path('homepage') }}" class="btn btn-primary">Back</a>
        {% endif %}
    </div>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}